-- Vérifier la structure actuelle de la table objectifs
DESCRIBE objectifs;

-- Voir toutes les colonnes de la table
SHOW COLUMNS FROM objectifs;

-- Vérifier les données existantes
SELECT * FROM objectifs LIMIT 5;
